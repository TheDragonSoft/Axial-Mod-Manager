export default function BrowsePage() {
  return (
    <div>
      <h2 className="text-lg font-semibold text-zinc-100">Browse</h2>
      <p className="mt-2 text-sm text-zinc-500">
        Mod index search arrives in Phase 2 (mock data), live in Phase 4.
      </p>
    </div>
  );
}
